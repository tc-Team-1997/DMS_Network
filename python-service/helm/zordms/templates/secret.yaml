apiVersion: v1
kind: Secret
metadata:
  name: {{ include "zordms.fullname" . }}-secrets
  labels: {{- include "zordms.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $k, $v := .Values.secrets }}
  {{ $k }}: "{{ $v }}"
  {{- end }}
