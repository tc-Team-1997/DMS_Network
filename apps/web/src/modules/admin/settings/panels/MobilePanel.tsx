import { ConfigPanel } from '../ConfigPanel';

export function MobilePanel() {
  return (
    <ConfigPanel
      namespace="mobile"
      title="Mobile"
      description="Branch-officer mobile capture app configuration. Mobile wave modules will publish this schema."
    />
  );
}
